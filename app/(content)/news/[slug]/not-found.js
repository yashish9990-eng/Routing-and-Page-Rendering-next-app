export default function NewsNotFoundPage({ params }) {
  return (
    <div id="error">
      <h1>Not Found!</h1>
      <p> Unfortunately, we could not be find the requested article.</p>
    </div>
  );
}
